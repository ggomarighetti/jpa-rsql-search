signing:
  active: ALWAYS
  pgp:
    active: ALWAYS
    armored: true

deploy:
  maven:
    mavenCentral:
      sonatype:
        active: RELEASE
        url: https://central.sonatype.com/api/v1/publisher
        namespace: io.github.ggomarighetti
        applyMavenCentralRules: true
        stagingRepositories:
          - target/staging-deploy
